export const authRoute = [
  {
    label: 'Login',
    path: '/login',
    // component: React.lazy(() => import('../pages/Auth/NewLogin/LoginPage')),
    // component: React.lazy(() => import('../pages/Auth/Login/LoginPage')),
    header: false,
  },
];

// Main application routes
export const mainRoute = [
  {
    label: 'OTP Verification',
    path: '/otp-verification',
    // component: React.lazy(() => import('../pages/Auth/OTPEntryPage/OTPEntryPage')),
    header: false,
  },
];

export const commonRoute = [
];
